<?php
/**
 * NPad — Persian notepad.
 *
 * A real URL rather than a CSS-toggled section, so /fa/ can be crawled,
 * linked and cached independently.
 */

declare(strict_types=1);

$lang = 'fa';
require dirname(__DIR__) . '/includes/page.php';
