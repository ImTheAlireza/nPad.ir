# NPad

# Credentials — never commit
config.php
.env
.env.*

# Logs
*.log
error_log

# Dependencies (test tooling only; the site itself ships no JS deps)
node_modules/
# package-lock.json is committed on purpose: CI runs npm ci.

# Test output
tests/rendered/

# Runtime content-date cache written by npad_content_lastmod()
.content-dates.json

# Backups / dumps
*.bak
*.old
*.backup
*.sql
*.sqlite

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~
